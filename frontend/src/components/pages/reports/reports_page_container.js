import { connect } from "react-redux";
import ReportsPage from "./reports_splash_page";

const mstp = state => {
  return {};
};

const mdtp = dispatch => {
  return {};
};

export default connect(
  mstp,
  mdtp
)(ReportsPage);