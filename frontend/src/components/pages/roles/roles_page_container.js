import { connect } from "react-redux";
import RolesPage from "./roles_page";

const mstp = state => {
  return {};
};

const mdtp = dispatch => {
  return {};
};

export default connect(
  mstp,
  mdtp
)(RolesPage);
